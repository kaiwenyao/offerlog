# OfferLogs 求职追踪系统：全栈开发计划

更新日期：2026-09-05  
交付目标：可部署到个人服务器的求职管理 Web 应用，后端使用 Go，附件接入 SeaweedFS 的 S3 API，桌面与手机均可使用。

## 1. 产品定位与范围

系统需要随时回答：有哪些想投的岗位、哪些已经投递、目前走到哪里、下一步该做什么，以及哪些渠道和投递策略更有效。

默认按个人使用设计：首版单账号、关闭公开注册，但所有业务数据都带 owner_id，避免未来扩展时重构归属关系。部署采用 Docker Compose；不依赖第三方 SaaS 才能运行。日期按用户时区展示，默认可设 Europe/Dublin，界面默认中文。

### 1.1 首版必须交付

- 岗位收藏、准备、投递、面试、Offer、拒绝、撤回等完整记录。
- 表格、看板、移动端列表视图，共用一份数据；保存筛选、排序、分组及列配置。
- 公司、岗位链接、JD 快照、渠道、优先级、截止日期、联系人、备注与附件。
- 进度变更历史、面试轮次、下一步行动及站内到期提醒。
- 简历等文件上传、下载、关联记录，存入私有 SeaweedFS bucket。
- 统计看板、当前进度桑基图、历史路径桑基图、图表明细下钻。
- 基础自定义属性、CSV 导入导出、完整数据备份与恢复流程。
- 登录、响应式布局、错误恢复、自动化测试及部署文档。

### 1.2 后续迭代

日历视图、复杂公式与跨表 Rollup、邮件自动识别、浏览器收藏扩展、多用户协作、外部通知、全文附件检索、超大文件分片上传、PWA 离线编辑。首版不以复刻完整 Notion 为目标，优先把求职流程做完整。

## 2. 核心概念和业务规则

### 2.1 记录粒度

一条 application 代表“一次岗位申请机会”，从收藏开始存在，不必等到已投递。一个公司可以有多个岗位；同一岗位重新投递创建新记录，并通过 previous_application_id 关联，保证每次尝试分别统计。公司名称相同、岗位链接相同只触发重复提示，不直接禁止保存。

### 2.2 标准状态

| 状态键 | 中文 | 分类 | 主要操作 |
| --- | --- | --- | --- |
| saved | 待投递 | 准备中 | 填写岗位、设置截止时间 |
| preparing | 准备材料 | 准备中 | 关联简历、求职信 |
| applied | 已投递 | 进行中 | 记录投递时间和渠道 |
| screening | 初筛 / 沟通 | 进行中 | 记录回复和下一步 |
| assessment | 笔试 / 作业 | 进行中 | 记录截止时间 |
| interviewing | 面试中 | 进行中 | 创建一面、二面等轮次 |
| offer | 收到 Offer | 待决策 | 记录条件和答复期限 |
| accepted | 已接受 | 已结束 | 记录接受时间 |
| rejected | 被拒绝 | 已结束 | 可填拒绝原因 |
| withdrawn | 已撤回 | 已结束 | 区分投递前放弃和投递后撤回 |
| closed | 岗位关闭 | 已结束 | 可填关闭原因 |

- 状态不是严格的单向流水线：允许跳过笔试、回到沟通阶段、终态重新打开；异常转换要求填写原因。
- accepted 要求有 Offer 记录或同时补录收到 Offer 事件；从准备中直接进入后续招聘阶段时，必须补充实际投递时间或标记“未经过正式投递”。不伪造中间阶段。
- 面试轮次独立存储，不用“一面、二面、三面”扩张顶层状态枚举。
- “暂无回复”由投递时间、first_response_at 和可配置天数推导；不自动判定拒绝。自动回执不算有效回复，first_response_at 由用户确认。
- 归档和软删除是可见性属性，不是招聘状态；归档默认保留统计，软删除默认排除。
- 每次真实状态变化写一条事件，包含旧状态、新状态、业务发生时间 occurred_at、系统记录时间 recorded_at、原因和操作者。当前状态与事件在同一事务更新。
- 误操作撤销通过新增纠正事件关联原事件，保留审计痕迹；统计从有效业务事件重新计算。补录历史只允许在预览最终时间线并通过一致性校验后保存。
- 相同时间的事件使用记录内递增 sequence 确定顺序。创建记录也生成初始状态事件。

### 2.3 主流程验收示例

收藏岗位 → 标记准备材料 → 上传并关联简历版本 → 标记已投递 → 补充沟通记录 → 安排两轮面试 → 收到 Offer → 接受或撤回。任一步刷新页面均能恢复；列表、详情、看板、统计结果一致。

## 3. 类 Notion 数据库功能

借鉴 Notion 的“同一数据、多种视图、独立筛选和属性显示”模式；岗位详情相当于数据库行展开页。参考：[Notion 视图、筛选、排序与分组](https://www.notion.com/help/views-filters-and-sorts)。以下范围为本项目设计。

### 3.1 内置字段

公司、岗位名称、岗位 URL、JD 文本快照、地点、远程方式、雇佣类型、薪资上下限及币种、渠道、状态、优先级、标签、收藏时间、投递时间、有效回复时间、截止时间、下一步行动、负责人（内部归属字段）、备注、关联文件。

不同币种不直接相加或比较；首版按币种分组展示，不做隐含汇率换算。纯日期截止时间使用 date；面试等精确时间使用 timestamptz，并保存事件时区。

### 3.2 属性扩展

首版支持文本、数字、单选、多选、日期、复选框、URL。核心流程字段固定，自定义字段存储于 JSONB，由独立字段定义表限制类型、长度、选项 ID。选项重命名不影响历史值；删除选项前显示受影响记录数量并允许映射；有数据的字段变更类型必须执行显式转换。

暂不提供任意公式执行、跨数据库关系和 Rollup。常用派生信息如等待天数、停留天数由后端提供。

### 3.3 视图和操作

- 表格：服务端分页、列显示/隐藏、列宽与顺序、单元格编辑、行选择及有限批量操作。
- 看板：默认按状态分组；拖拽变更状态时遵循业务规则，也提供点击菜单和键盘操作。
- 列表：适合手机，每项突出公司、岗位、状态、下一步和截止时间。
- 保存视图：名称、布局、字段配置、筛选树、排序、分组、视图配置版本。
- 筛选支持 AND/OR 组合；首版限制嵌套深度 3、条件数 30，防止难以维护的查询。
- 内置视图：全部机会、待投递、本周待办、进行中、收到 Offer、已结束。
- 筛选后顶部明确显示“共 X 条，已应用 Y 个条件”；视图切换保留合理的滚动位置。
- 首版批量支持标签、优先级、归档；批量状态变更后置，避免跳过每条记录必要的补录字段。

筛选 DSL 必须校验字段 ID、操作符、类型和权限，再转换为参数化 SQL；不接受前端传入 SQL、字段表达式或任意排序片段。保存视图与统计筛选复用同一解释器。

## 4. UI/UX 与响应式设计

### 4.1 设计方向

选择“清晰的求职工作台”：进入后先看到今天需要处理的事情，再看完整数据库和分析。曾考虑以大数字看板作为首页，但它不利于决定下一步，因此首页采用行动清单；独立分析页承载图表。

视觉识别点是“阶段轨迹”：在详情页和记录展开态中，以细线连接真实走过的阶段，当前阶段突出，未经过阶段不填充；与桑基图使用一致的状态颜色。列表保持紧凑，不给每行堆满轨迹装饰。

| Token | 建议值 | 用途 |
| --- | --- | --- |
| canvas | #F5F7FA | 页面底色 |
| surface | #FFFFFF | 表格、面板 |
| ink | #18263A | 正文及标题 |
| primary | #2855D9 | 主操作、当前阶段 |
| positive | #147D70 | Offer / 接受状态 |
| attention | #A64826 | 截止和需处理提示 |

状态同时显示文字和图标，不能只靠颜色区分。实际组件开发时检查 WCAG AA 对比度，尤其浅色状态标签和禁用态。标题使用 Manrope 搭配 Noto Sans SC，正文使用系统 UI 字体搭配中文系统字体，数字使用等宽数字特性；字体本地托管或使用系统回退。标题约 24–28px，正文 14–16px，行高约 1.5，间距以 4/8px 为基准，面板圆角 8px。

### 4.2 信息架构

侧栏：今日待办 / 求职数据库 / 统计分析 / 文件库 / 设置。桌面保留 220px 左栏；数据库主区域独立滚动；详情可在约 480–640px 侧面板打开，也有可分享的完整详情路由。

```text
桌面：数据库
┌────────────┬───────────────────────────────────────────┐
│ OfferLogs  │ 求职数据库                     + 新增岗位 │
│ 今日待办   │ 全部  待投递  进行中  Offer      表格 ▾    │
│ 求职数据库 │ 搜索…     筛选(2)   排序   属性            │
│ 统计分析   ├───────────────────────────────────────────┤
│ 文件库     │ 公司 / 岗位     状态     下一步    截止    │
│ 设置       │ Northstar       面试中   准备二面  09/08   │
└────────────┴───────────────────────────────────────────┘

手机：求职列表
┌───────────────────────────┐
│ 求职数据库           ＋    │
│ 搜索…          筛选(2)    │
│ 全部  待投递  进行中       │
│ Northstar · 后端工程师     │
│ 面试中     09/08 准备二面  │
│ 查看详情     更新进度      │
├───────────────────────────┤
│ 待办   岗位   分析   更多  │
└───────────────────────────┘
```

### 4.3 页面与交互要求

- 今日待办：逾期、今天、本周分组；明确下一步行动，可一键完成或改期。
- 新增岗位：先只要求公司和岗位，链接可选；高级字段折叠，保存后可继续编辑。
- 详情：岗位摘要、阶段轨迹、下一步行动、面试、附件、时间线和备注；更新时间无需进入完整编辑表单。
- 单元格编辑：Enter 保存、Esc 取消；请求中显示保存状态，失败保留输入并允许重试。
- 更新进度弹出精简表单：目标状态、发生时间、必要字段和可选说明；乐观更新失败回滚，冲突提示重新加载。
- 附件显示原文件名、类别、大小、上传状态和所关联的简历版本；移动端选择文件后可以观察进度并取消。
- 空数据引导“添加第一个岗位”或“导入 CSV”；筛选无结果提供“清除筛选”。异常显示可操作信息，不只显示错误码。

### 4.4 响应式与可访问性

| 宽度 | 行为 |
| --- | --- |
| 320–767px | 默认卡片列表、底部导航、详情全屏、筛选底部抽屉 |
| 768–1199px | 折叠侧栏、简化列、可切换详情页 |
| ≥1200px | 完整侧栏、表格/看板、侧面详情 |

手机默认不使用整页横向滚动；用户主动选择表格或完整桑基图时，仅该组件允许横向滚动。看板手机按阶段切换，跨阶段修改提供菜单。触摸目标至少 44×44 CSS px；对话框有焦点管理和 Esc 关闭；所有核心流程键盘可完成；尊重 reduced-motion 和安全区。

桑基图在手机先显示简化分支和对应数量表，可打开全屏图；点击节点替代悬停提示。图表提供文本摘要、明细表以及键盘可操作的下钻入口。

## 5. 数据看板与桑基图口径

### 5.1 公共约定

统计单位默认是 application 去重记录数。页面固定显示筛选条件、样本量、时间范围、业务时区、统计截止时刻 as_of 和口径版本。首版 as_of 为本次计算时间，不提供历史数据库快照查询；历史事件补录会影响重新计算结果。

当前分布默认按收藏日期选样；投递效果默认按 submitted_at 选取 cohort（同一时间段实际投递的申请集合），再查看该集合截至当前的结果。选择投递日期的 cohort 时，未投递记录自然不包含在样本内，界面明确说明。事件发生趋势按事件日期统计，不能与 cohort 转化率混用。

### 5.2 指标定义

| 指标 | 定义 |
| --- | --- |
| 待投递 | 当前 saved / preparing 数量 |
| 已投递 | submitted_at 非空的去重申请数，包含已结束申请 |
| 进行中 | 当前 applied / screening / assessment / interviewing 数量 |
| 有结果 | 当前 offer / accepted / rejected / withdrawn / closed，按类型细分 |
| 有效回复率 | 投递 cohort 中 first_response_at 非空数量 / cohort 总数 |
| 面试到达率 | 投递 cohort 中曾有效进入 interviewing 的数量 / cohort 总数 |
| Offer 率 | 投递 cohort 中曾收到 Offer 的数量 / cohort 总数 |
| 首次回复耗时 | submitted_at 到首次有效回复的时长中位数，只纳入已回复样本 |
| 当前阶段停留 | 最近一次进入当前状态到 as_of 的时长，不把前一次停留累加 |
| 渠道效果 | 每个渠道的投递量、有效回复率、面试到达率、Offer 率 |

“有结果”包含主动撤回等管理结果，不能当作“收到招聘方回复”；卡片说明此区别。分母为零显示“—”；耗时旁显示已回复样本数和未回复数；样本少于 10 条显示样本偏少提示。状态终结后回复率和历史到达率不因当前状态改变而丢失。

看板顺序：关键数量 → 当前进度桑基图 → 投递及回复趋势 → 渠道效果 → 等待时间 / 待跟进列表。点击卡片、图表节点和边都能打开数量一致的记录列表。

### 5.3 桑基图 A：当前进度，默认视图

采用固定三层有向无环图：

`全部机会 → 是否已投递 → 当前状态`

中间层区分“尚未投递”和“已投递”，依据 submitted_at 是否存在；节点 ID 包含父分组，避免未投递撤回与已投递撤回误合并。每条申请在每层只出现一次。节点/边数值、下钻与卡片共用同一筛选和数据库快照。

基准数据共 10 条：待投递 2、准备材料 1、已投递等待 2、面试中 2、Offer 1、拒绝 1、投递后撤回 1。应产生全部机会 10 → 尚未投递 3 / 已投递 7；未投递分支为 2+1，已投递分支为 2+2+1+1+1。每个非终点节点入流等于出流，各最终节点合计 10。

这是当前快照，不声称展示历史顺序或阶段转化率。用户可以一眼看到哪些还没投以及已投递的去向。

### 5.4 桑基图 B：实际历史路径

从每条申请的有效状态事件重建路径，使用 `(step_index, status)` 作为节点 ID，并从唯一根节点进入。一个状态可以在不同层重复出现；边只连接相邻步骤，因此“面试 → 沟通 → 面试”不会产生循环。此设计与 ECharts 的 DAG 布局约束相适配，参考其[桑基图布局实现](https://github.com/apache/echarts/blob/master/src/chart/sankey/sankeyLayout.ts)。

- 只画实际发生的阶段；跳过笔试不会补一条虚构的笔试边。
- 连续同状态备注不算状态迁移；误操作纠正先解析再生成路径。
- 每条路径末端增加“截至当前：某状态”终点，确保中途仍在等待的样本也有去向。
- 默认最多展示 12 个事件步骤，更长路径进入“后续步骤已折叠”终点；通过下钻查看完整历史，禁止直接丢弃尾部样本。
- 边宽表示经过该边的去重申请数量。同一申请可以出现在不同步骤的边上，不能把全图所有边求和当总投递量。
- 节点同时显示步骤和阶段名称；阶段到达率由另一个去重查询计算，不用图中重复节点相加。
- 缺少历史的导入记录显示“导入起点 → 已知当前状态”，不推断曾经历的招聘阶段。
- 节点合并后不保证单条完整路径仍可视觉辨认，明细侧栏提供真实时间线。

API 返回 nodes、links、cohort_count、as_of、definition_version、筛选摘要和可复现下钻令牌；令牌绑定用户、模式、筛选和统计时刻，不把大量记录 ID 塞进 URL。短期保存统计成员集合，令牌过期后提示刷新，避免用户更新数据后旧图与新明细数量矛盾。

图表支持 SVG/PNG 导出；两种模式均支持节点和边的 CSV 数据导出，并附带口径和样本量。

## 6. 技术方案与系统边界

以下为建议选型，开工时固定受支持的稳定版本及镜像 digest，不使用浮动 latest 标签。

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 前端 | React + TypeScript + Vite | 私有工作台使用 SPA，无需 SEO 服务端渲染 |
| 路由 / 请求 | React Router + TanStack Query | 详情深链接、请求缓存及保存后失效管理 |
| 组件 / 样式 | Radix primitives + Tailwind CSS | 自建一致视觉组件，保留可访问交互基础 |
| 表格 / 表单 | TanStack Table + React Hook Form + Zod | 服务端列表状态、类型校验和复杂表单 |
| 图表 | Apache ECharts，按需加载 | 统一桑基图、趋势及分布图 |
| 后端 | Go + net/http + chi | 保持标准 HTTP 生态和轻量路由 |
| 数据库 | PostgreSQL + pgx + sqlc | SQL 明确、事务可见、查询结果类型化 |
| 迁移 | goose | 版本化数据库迁移 |
| 文件 | AWS SDK for Go v2 → SeaweedFS S3 | 将对象存储封装为可替换适配器 |
| 后台任务 | Go worker + PostgreSQL jobs/outbox | 首版无需额外 Redis / 消息队列 |
| API 契约 | OpenAPI 3.1 + 生成 TypeScript 类型 | 接口先约定，前后端共享契约 |
| 部署 | Docker Compose + Caddy | 单机部署、HTTPS、反向代理 |

```text
浏览器
  └─ HTTPS → Caddy
                ├─ /        前端静态文件
                └─ /api/v1  Go API
                               ├─ PostgreSQL
                               └─ SeaweedFS S3 Gateway → Filer → Volume
                  Go Worker ───┴─ 清理、提醒、导入与导出任务

可选：浏览器通过 files.example.com 的受限入口直传临时对象。
```

后端采用模块化单体。HTTP 层负责协议和鉴权上下文；应用服务负责用例、权限与事务；领域层负责状态规则及统计语义；适配器负责 SQL 和 S3。业务模块不得直接调用其他模块的 HTTP handler，领域代码不得依赖 Web 框架或 AWS SDK。

## 7. Go 后端目录与责任

```text
offerlogs/
├── backend/
│   ├── cmd/
│   │   ├── api/main.go
│   │   ├── worker/main.go
│   │   └── admin/main.go          # 初始化账号、恢复辅助操作
│   ├── internal/
│   │   ├── bootstrap/            # 显式依赖组装、启动关闭
│   │   ├── platform/
│   │   │   ├── config/
│   │   │   ├── database/         # 连接、事务管理
│   │   │   ├── objectstore/      # S3 适配器
│   │   │   ├── jobs/
│   │   │   └── observability/    # 日志、指标、request ID
│   │   ├── identity/
│   │   ├── companies/
│   │   ├── applications/
│   │   │   ├── domain/          # 状态、事件、不变量
│   │   │   ├── service/         # 创建、更新、转换、纠正
│   │   │   ├── repository/      # SQL 适配
│   │   │   └── transport/http/  # DTO、handler、路由
│   │   ├── activities/          # 面试、行动、提醒
│   │   ├── files/
│   │   ├── views/               # 属性定义、筛选 DSL
│   │   ├── analytics/           # 指标及桑基图投影
│   │   └── transfers/           # 导入导出
│   ├── db/{migrations,queries,generated}/
│   └── tests/{integration,contract,fixtures}/
├── frontend/
│   ├── src/{app,components,features,lib,styles}/
│   └── tests/{e2e,fixtures}/
├── api/openapi.yaml
├── deploy/{compose.yaml,Caddyfile,.env.example}/
├── docs/{architecture,runbooks,decisions}/
└── Makefile
```

其他业务模块按复杂度采用相同分层，小模块无需创建空目录。接口由调用方按实际需要定义，不为每个结构体机械增加接口。数据库生成代码只存在于适配层；不让 HTTP DTO、数据库行结构体和领域对象互相替代。依赖通过构造函数传入，禁止全局可变数据库连接和循环依赖。

状态转换、事件写入、当前快照更新、必要任务 outbox 写入在一个数据库事务内完成。外部 S3 调用不能放在持锁事务中；采用持久化状态和重试实现最终一致性。worker 使用租约、attempts、next_run_at 和幂等键；失败达到上限后可在运维命令中查询及重试。

## 8. 数据模型与约束

| 表 | 关键内容 |
| --- | --- |
| users / sessions | 账号、密码哈希、时区、服务端会话及到期时间 |
| companies | owner_id、名称、网站、备注 |
| applications | 公司、岗位、当前状态、投递时间、版本号、核心字段、custom_values JSONB、归档/删除时间 |
| application_events | 记录内序列、事件类型、前后状态、发生/记录时间、纠正关联、原因 |
| interviews | 轮次、形式、时间、时区、结果、反馈、关联申请 |
| actions | 下一步内容、到期日期/时间、完成时间、提醒规则 |
| notes | 申请关联、Markdown 内容、创建/修改时间 |
| property_definitions | 自定义字段类型、选项、校验、schema_version |
| saved_views | 布局、filter_ast、sort、group、columns、schema_version |
| files | 对象 key、原名、MIME、大小、SHA-256、状态、类别、创建时间 |
| application_files | 申请与附件关联、用途、简历版本标记 |
| jobs / outbox | 持久化任务、幂等键、租约、重试状态 |
| import_batches | 字段映射、预检报告、处理结果和重复处理策略 |
| analytics_snapshots | 短期统计快照及下钻成员，过期自动清理 |

每张业务表记录 owner_id 与时间；跨表关联通过复合归属约束或同一事务中的归属校验防止跨账号引用。索引优先覆盖 `(owner_id, status)`、`(owner_id, submitted_at)`、`(owner_id, next_action_due_at)`、事件的 `(application_id, occurred_at, sequence)`。自定义属性仅对真实高频查询追加表达式索引，不预建所有索引。

application 使用整数 version 乐观锁；旧版本修改返回 409。更新状态使用 idempotency_key，重复请求不重复生成事件。分页排序末尾加 id，确保稳定；默认每页 50 条，最大 100 条。全文搜索首版限定岗位、公司、标签和备注，中文使用可验证的子串方案，搜索规模扩大后再评估专用索引。

## 9. API 规划

统一前缀 `/api/v1`；使用 JSON；错误结构 `{code, message, field_errors, request_id}`，不暴露 SQL 或内部堆栈。所有业务读取、附件关联和图表下钻都校验归属。

| API | 职责 |
| --- | --- |
| POST /auth/login；POST /auth/logout；GET /me | 会话 |
| GET/POST /applications | 简单列表、创建 |
| POST /applications/query | 有嵌套筛选的查询请求 |
| GET/PATCH/DELETE /applications/{id} | 详情、普通字段更新、软删除 |
| POST /applications/{id}/transitions | 状态变更，携带 version / idempotency_key |
| GET /applications/{id}/events | 时间线 |
| POST /applications/{id}/corrections | 有审计记录的历史纠正 |
| POST /applications/{id}/restore | 从回收站恢复 |
| GET/POST/PATCH /companies、/views、/properties | 各资源列表、创建及按 ID 更新 |
| /applications/{id}/interviews、/actions、/notes | 按归属进行增删改查 |
| POST /files | 首版流式上传 |
| GET /files/{id}/download | 鉴权后下载或签发短时 URL |
| POST/DELETE /applications/{id}/files/{file_id} | 建立或移除文件关联 |
| DELETE /files/{id} | 删除文件，校验引用情况 |
| POST /analytics/summary、/trends、/sankey | 共用筛选输入，返回统计快照 |
| GET /analytics/drilldowns/{token} | 分页查看图表成员 |
| POST /imports/preview；POST /imports/{id}/commit | 导入预检及确认执行 |
| POST /exports；GET /jobs/{id} | 导出与长任务状态 |

此表为接口范围，OpenAPI 中补齐具体子资源路径、schema、分页和错误码。输入严格限制字符串长度、数组数量、日期范围及请求大小。新增岗位链接只作为外链保存，首版不由服务器自动抓取 JD，避免引入网页解析和网络访问边界。

## 10. SeaweedFS / S3 文件方案

SeaweedFS 的 S3 gateway 位于 Filer 之前，首版通过标准对象操作访问。参考：[SeaweedFS S3 API](https://github.com/seaweedfs/seaweedfs/wiki/Amazon-S3-API)。兼容性以部署时固定的 SeaweedFS 镜像和 SDK 组合实测为准。

### 10.1 首版上传策略：Go 流式代理

个人求职文件体积通常有限，首版默认单文件最大 20 MiB，通过 Go API 流式传输至 S3，严格限制请求体和并发，不将整个文件读入内存。这能在单域名部署下控制大小、类型及鉴权，减少直传配置成本。

流程：鉴权及额度预留 → 创建 pending 文件记录 → 流式读取并识别 MIME、计算 SHA-256 → 上传私有 staging key → 验证实际大小和类型 → 提升至独立 final key → 数据库设为 ready → 异步清理 staging。final key 不使用原文件名，结构如 `owners/{owner_id}/files/{uuid}/content`。

允许 PDF、DOCX、TXT、PNG、JPEG；DOCX 需按 ZIP 容器验证类型和解压限制，而非只检查通用 ZIP 文件头。首版不接受 HTML、SVG、可执行文件或宏文档；文件名仅用于展示及经过编码的下载响应头。预览文本按纯文本呈现，文档默认下载；PDF 如提供内嵌预览，应通过受限独立来源与 sandbox 实现。

文件状态为 pending → validating → ready，失败进入 failed，删除进入 deleting → deleted。只有 ready 文件能下载或关联。重试生成新临时对象，不覆盖既有简历；修改简历上传为新文件版本，保留历史投递使用的版本。

### 10.2 可选直传扩展

以后需要直传时增加 initiate / complete 两步接口，签发仅绑定一个 staging key、有效期约 5 分钟的预签名请求。配置 `S3_INTERNAL_ENDPOINT` 和 `S3_PUBLIC_ENDPOINT`：内部服务访问与浏览器实际签名域名分别处理，禁止签名后直接替换主机名。

complete 不能相信客户端“上传成功”：后端读取对象核验实际大小、类型、摘要，再复制至从未签发写权限的 final key，验证后标记 ready，避免尚未过期的上传链接改写已确认附件。预签名 PUT 本身不作为大小限制的唯一保障；受控入口设置请求上限，并在完成校验时清理超限对象。直传单独执行 CORS、签名、重放和失败恢复测试。

### 10.3 配置、安全与一致性

- 使用 AWS SDK for Go v2，配置 endpoint、region、credentials 和 path-style 访问；参考 [SDK endpoint 配置](https://docs.aws.amazon.com/sdk-for-go/v2/developer-guide/configure-endpoints.html)。SDK 校验和及请求编码行为纳入与真实 SeaweedFS 的集成测试。
- bucket 禁止匿名读写；凭证只在后端，使用受限 bucket 权限，不进入前端包或日志。
- 下载先校验 owner，默认通过 API 流式返回；可选签发约 60 秒的只读 URL。明确短时 URL 在到期前可能仍可用，不承诺即时撤销。
- 移除申请与文件的关联不等于删除文件；被其他申请引用时禁止直接删除，提示先处理引用。
- 删除写入持久化清理任务；S3 删除幂等重试，数据库保留任务结果。
- 定时清理过期 pending/staging 对象，复核无引用再清理孤儿 final 对象；宽限期默认 24 小时，避免与在途请求竞争。
- S3 故障时，岗位管理仍可使用；上传显示明确失败，不能显示成功附件。服务重启后未完成任务可恢复。
- 配置总额度、单文件上限、每用户并发上限；额度预留和释放事务化，避免并发超额。

## 11. 自托管、登录与运维

提供两套 Compose profile：连接已有 SeaweedFS；启动自包含 SeaweedFS。自包含部署显式持久化 Master 元信息、Volume 数据、Filer 元数据存储以及 PostgreSQL；不能只挂载 volume 文件目录。存储副本数按实际节点数量配置，单机副本不视为异地备份。

生产入口只有 HTTPS；数据库、Master、Volume、Filer 及内部 S3 端口不暴露公网。直传扩展只开放受限 S3 域名入口。反向代理保留签名所需 Host、路径、查询和必要请求头，设置上传大小及超时。

管理员通过 CLI 初始化账号，无固定默认密码；使用 Argon2id 密码哈希、HttpOnly/Secure/SameSite cookie 和服务端会话；写请求校验 CSRF token / Origin。登录限制频率；日志去除密码、cookie、预签名 URL、简历内容等敏感信息。

配置通过环境变量或挂载 secret 文件注入，提供 `.env.example`。API 启动前执行一次独立迁移任务，不由多个进程同时迁移。`/health/live` 检查进程，`/health/ready` 检查数据库和 schema；对象存储故障另行报告文件服务降级，不直接停掉全部岗位功能。

备份目标初定 RPO 24 小时、RTO 2 小时，需在实机演练验证。每日加密备份 PostgreSQL 与 SeaweedFS 必需的对象及元数据到另一物理位置。首版采用维护窗口暂停写入和清理任务，等待在途上传结束后生成同一备份批次；同时保存清单、schema 版本及对象摘要，避免数据库与文件时间点错位。

恢复演练要从空环境恢复，校验申请数量、随机附件摘要、附件关联、状态事件和看板结果。每月演练一次。部署回滚使用上一镜像；数据库变更采用兼容的 expand/contract，破坏性迁移必须先有可恢复备份，不能假定 down migration 不丢数据。

初始资源预算按个人低并发估计 2–4 vCPU、4–8 GB RAM，实际容量由附件与 SeaweedFS 配置决定；这只是预算假设，上线前以目标服务器测试结果调整。

## 12. 测试计划与发布门槛

测试同时验证业务规则、真实依赖、用户操作和运维恢复；S3 mock 不能替代 SeaweedFS 集成测试。本文定义未来实施要求，不表示系统或测试已实现。

### 12.1 测试矩阵

| 层级 | 工具 / 环境 | 必须覆盖 | 通过条件 |
| --- | --- | --- | --- |
| Go 单元测试 | testing，表驱动及 fuzz | 状态跳转、纠正、时区、筛选校验、指标分母 | 核心业务路径通过；domain / analytics 语句覆盖率目标 ≥85% |
| 后端集成 | 真实 PostgreSQL + 固定 SeaweedFS 容器 | 事务、并发冲突、对象上传下载删除、重试 | 每次 CI 独立环境通过，无外部账号依赖 |
| API 契约 | OpenAPI lint + 请求/响应校验 | 错误码、分页、生成类型、鉴权 | 契约和实现一致，生成文件无未提交差异 |
| 前端组件 | Vitest + Testing Library | 编辑保存、失败保留输入、筛选、上传状态、空态 | 关键交互断言通过 |
| 端到端 | Playwright | 新增到 Offer 全流程、刷新持久化、导入导出、下钻 | Chromium 全套；Firefox / WebKit 核心流程通过 |
| 可访问性 / 视觉 | axe + 截图回归 + 人工键盘检查 | 焦点、对比、响应式、图表替代信息 | 关键页面无严重问题；主要路径键盘可操作 |
| 性能 | k6 + 浏览器性能分析 | 大列表、筛选、统计、附件并发 | 满足下述量化目标 |
| 运维验收 | Compose 隔离环境 | 首次部署、升级、重启、备份恢复 | 从文档可复现，不依赖开发机残留数据 |

### 12.2 必测业务与统计案例

1. 10 条基准数据：当前桑基图各层守恒，所有终点总和为 10，下钻返回完全相同 ID 集合。
2. 历史出现“面试 → 沟通 → 面试”：图无环、无递归错误，同一阶段到达率只计一次。
3. 初筛后直接 Offer：不生成笔试或面试事件；收到 Offer 后拒绝或撤回，历史 Offer 率仍保留。
4. 重复提交相同 idempotency_key：只产生一次转换；两个标签页使用旧 version 更新返回 409。
5. 终态重开、误操作撤销、历史补录、同时间事件、超过 12 步、导入缺历史均有明确期望结果。
6. 归档仍被统计；软删除被排除；恢复重新纳入。空样本、分母为零不显示 NaN 或误导性的 0%。
7. 跨月、午夜、Europe/Dublin 夏令时切换、纯日期截止均不丢失或重复计数。
8. 图生成后修改记录：旧图下钻仍按快照；快照过期要求刷新，重新生成后一致。

### 12.3 必测文件与权限案例

- 未登录、伪造文件 ID、跨 owner 下载/关联、匿名 bucket 访问全部被拒绝；即使首版单账号也使用双用户测试 fixture。
- 实际超限、假扩展名、DOCX 异常压缩包、零字节、中文及特殊字符文件名均得到预期处理。
- 上传中断、S3 不可用、数据库最终提交失败、worker 重启、重复删除和额度并发：没有可见的坏附件，后续任务能清理残留。
- 完整文件往返计算 SHA-256 相等；不把 multipart ETag 当作内容摘要。
- 直传如进入发布范围，额外测试公开 endpoint 签名、CORS、过期链接、重复上传改写 staging，以及 ready 的 final 文件不受影响。
- 登录暴力尝试限流、CSRF、备注 XSS、筛选注入、CSV 公式注入均有自动化用例；CSV 导出对危险前缀采取明确的电子表格安全转义。

### 12.4 性能与移动端目标

测试数据：单账号 10,000 条申请、100,000 条事件；在记录清楚的 2 vCPU / 4 GB 测试环境和 20 个并发会话下，普通列表/详情服务端 p95 <300ms，复杂筛选 p95 <800ms，统计 p95 <1.5s。上传单独测量，不计入普通 API 延迟；3 个并发 20 MiB 上传时确认内存不随文件总大小线性增长。

前端按页面拆包，ECharts 在分析页加载；模拟移动网络下关键列表 LCP 目标 <2.5s、CLS <0.1。桌面 1440×900、平板 768×1024、手机 390×844 和 320px 宽截图验收；真实 iOS Safari / Android Chrome 至少各检查文件选择、输入键盘遮挡、抽屉、导航和图表点击一次。仿真浏览器不能完全替代真机验收。

### 12.5 CI 与发布清单

每个 PR：格式、静态检查、Go 测试、TypeScript 类型检查、组件测试、OpenAPI 校验、前后端构建。影响数据库、文件、鉴权或统计的变更必须跑对应真实集成测试；发布分支跑完整 E2E、依赖漏洞检查及镜像构建。

发布前必须满足：关键测试全部通过；无未解释的严重安全问题；数据库迁移从上一版本演练成功；备份恢复演练成功；核心移动端流程验收；部署文档、配置样例和口径说明与版本一致。性能目标未达到时必须记录测量与修正结果，不能直接标记通过。

## 13. 实施阶段与交付物

估算按一名熟悉 Go / React 的开发者全职工作，约 6–8 周；不是承诺工期，S3 环境和真实使用反馈可能调整顺序。

| 阶段 | 参考时间 | 交付物与验收 |
| --- | --- | --- |
| P0 设计与风险验证 | 3–4 天 | 数据模型、状态规则、OpenAPI 草案、桌面/手机线框、真实 SeaweedFS 文件往返验证 |
| P1 工程和基础流程 | 5–7 天 | Compose、迁移、登录、公司与申请 CRUD、状态历史、事务和权限测试 |
| P2 数据库体验 | 6–8 天 | 表格/看板/列表、保存视图、自定义属性、详情、行动和面试记录、响应式验收 |
| P3 文件闭环 | 3–5 天 | 上传下载、版本关联、额度、失败恢复、清理任务、真实存储测试 |
| P4 分析闭环 | 5–7 天 | 看板、两类桑基图、快照下钻、统计基准 fixture、图表导出 |
| P5 数据迁移与上线 | 5–7 天 | CSV 预检导入、导出、E2E、性能修正、备份恢复、实机部署手册 |

P0 先验证 SeaweedFS，避免到 P3 才发现签名或 SDK 兼容问题。P1 起持续加入测试；P2 起每个功能同时验收手机。P4 依赖 P1 的事件模型，不能仅靠当前状态补算历史。

CSV 导入先预览字段映射、类型错误及重复候选；用户选择跳过或新建，不默认覆盖已有申请。批次提交幂等，可下载逐行错误报告。导入当前状态不伪造历史。业务 CSV 导出不等同于完整备份，备份还必须包含事件、视图、自定义字段定义和文件内容。

## 14. 完成标准

- 从空服务器按文档可部署，重启后数据和附件存在。
- 可完整追踪未投递到结束的机会，查看每次真实变化和使用过的材料版本。
- 同一记录在表格、看板、手机列表及详情中保持一致。
- 能按时间、渠道、标签和公司等条件分析，图表口径清楚，桑基图数量可与明细核对。
- 附件私有、归属可验证、失败可恢复，备份可从空环境还原。
- 前后端目录、依赖方向、API 契约和数据库迁移清晰，测试与发布门槛落地。

开始开发时需收集服务器架构及资源、域名、是否已有 SeaweedFS、S3 endpoint / bucket 配置、可用异地备份位置。凭证通过部署环境注入，不写进计划或代码仓库。这些信息不阻碍先完成 P0 的本地设计和容器验证。
